# Mini Offline Chatbot

## Objective
This project implements a simple offline chatbot that answers user queries from a small set of text files. It uses **TF-IDF vectorization** and **cosine similarity** to retrieve the most relevant paragraph from the corpus.

## Project Structure
